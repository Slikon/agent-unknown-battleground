{
  "name": "@aub/client",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Phaser 3 client — renders match state, sends player input.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@aub/shared": "workspace:*",
    "@colyseus/sdk": "^0.17.43",
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "vite": "^8.1.4"
  }
}
